#!/bin/bash

# Modify UFW configuration to enable port forwarding
sudo sh -c 'echo "*nat\n:PREROUTING ACCEPT [0:0]\n-A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80\nCOMMIT" >> /etc/ufw/before.rules'

# Reload UFW
sudo ufw reload

# Display UFW status
sudo ufw status
